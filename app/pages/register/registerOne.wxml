<view class="registerOne">
    <view class="tophints none">
        <text class="hint active">1.输入手机号 ></text>
        <text class="hint">2.输入验证码 ></text>
        <text class="hint">3.设置密码</text>
    </view>
    
   <view class="regOneInput">
    <input type="number" class="Reginput" maxlength="11"  value="{{inputValue}}" bindinput="checkPhone" disabled="{{disabled}}" placeholder="请输入手机号码"/>
     <view class="Clearinput"><image src="{{src}}" class=" {{showClose}}" mode="widthFix" bindtap="clearPhone"></image></view>
     </view>
     
      <view class="regTwoInput">   
    <input type="number" class="Reg2input " maxlength="6" placeholder="请输入短信验证码" bindinput="setMsgYzm"/>
    <button class="regTwo_countbtn {{receive}}">{{receiveYzm}}</button>
     <!--<button class="regTwo_countbtn {{receivea}}">获取验证码</button>灰色的验证码获取-->
    <button class="regTwo_countbtna {{receivea}}" bindtap="recAgain">获取验证码</button><!--红色的验证码获取-->
  </view>

<view class="hints">{{checkHint}}</view>

    <button class="regOnebtn btngrey {{showGrey}}"> 下一步</button>
    <button class="regOnebtn btngrey_act  {{showActive}}"  bindtap="gotoRegthree"> 下一步</button>

     <!--<view class="regone-container clear none">
        <text class="one">请输入验证码</text>
        <view class="two">
            <input type="number"  maxlength="4" value="{{yzmValue}}" bindinput="checkYzm"/>
            <image src="{{variimg}}" class="img" mode="widthFix" ></image>
            <text class="changeVari" bindtap="changeYzmfunc">换一张</text>
        </view>
        <text class="three" bindtap="gotoRegtwo">确定</text> 
     </view>-->

</view>